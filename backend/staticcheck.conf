checks = [
  "all",
  "-QF*",       # informational suggestions — not actionable
  "-ST1000",    # package comments — not enforced in this project
  "-ST1005",    # error string capitalization — excluded for CLI UX
  "-ST1022",    # comment form on exported vars — not enforced
  "-SA1019",    # deprecated API usage — websocket migration tracked separately
]
