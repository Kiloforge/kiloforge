# Specification: Update README Prerequisites to Docker + Claude Code

**Track ID:** readme-prerequisites_20260308210002Z
**Type:** Chore
**Created:** 2026-03-08T21:00:02Z
**Status:** Draft

## Summary

Update the README Prerequisites section to clearly state that Docker and Claude Code are the only runtime dependencies. Go and Node.js are only needed for building from source.

## Context

The README currently lists Go 1.24+ and Node.js 18+ as prerequisites alongside Docker and Claude Code. However, Go and Node are only needed to build the binary from source — they're not runtime dependencies. Users running a prebuilt binary only need Docker (for Gitea) and Claude Code (for agent spawning). The prerequisites should distinguish between runtime and build-time dependencies.

## Codebase Analysis

- **`README.md`** — Current Prerequisites section lists Docker, Claude Code, Go 1.24+, Node.js 18+ all at the same level
- The binary embeds the frontend (`//go:embed dist/*`), so Node.js is only needed at build time
- Go is only needed to compile the binary
- Docker is needed at runtime for Gitea
- Claude Code CLI is needed at runtime for agent spawning (`kf implement`)

## Acceptance Criteria

- [ ] README clearly separates runtime dependencies (Docker, Claude Code) from build dependencies (Go, Node.js)
- [ ] Runtime dependencies are listed first and prominently
- [ ] Build dependencies are in a separate subsection (e.g., "Building from Source")
- [ ] Colima note is preserved
- [ ] No other README content is changed

## Dependencies

None

## Out of Scope

- Restructuring other README sections
- Adding installation instructions for prebuilt binaries (no distribution mechanism yet)
- Updating the architecture diagram or command reference

## Technical Notes

Suggested structure:
```
## Prerequisites

- **Docker** with Docker Compose
- **Claude Code CLI** — `claude` command in PATH

### Building from Source

- **Go 1.24+**
- **Node.js 18+**

### Colima Users
...
```

---

_Generated by conductor-track-generator from prompt: "Update README prerequisites to Docker + Claude Code"_
