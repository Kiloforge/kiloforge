# Specification: Extract Domain Types, Port Interfaces, and Service Layer

**Track ID:** refactor-domain-ports_20260307140001Z
**Type:** Refactor
**Created:** 2026-03-07T14:00:01Z
**Status:** Draft

## Summary

Extract domain entities and sentinel errors into `internal/core/domain/`, define port interfaces in `internal/core/port/`, and extract business logic into `internal/core/service/`. Eliminate dependency direction violations so inner layers never import adapters.

## Context

After the package restructure (track refactor-clean-arch), the directory layout will be correct but the code still violates layering rules:
- Domain types (`Project`, `AgentInfo`, `PRTracking`) live in adapter packages
- No port interfaces exist for `ProjectStore`, `AgentStore`, `Logger`, `GiteaClient`
- Business logic is embedded in HTTP handlers (`relay/server.go`) and orchestration adapters
- Adapters import each other directly instead of through interfaces

This track introduces proper separation of concerns.

## Codebase Analysis

### Domain types to extract
- `Project` struct — currently in `internal/project/registry.go:15`
- `AgentInfo` struct — currently in `internal/state/state.go:16`
- `PRTracking` struct — currently in `internal/orchestration/tracking.go:13`
- `ProjectStatus`, `AgentRole` value objects — scattered or implicit

### Port interfaces to create
- `ProjectStore` — currently `project.Registry` used directly
- `AgentStore` — currently `state.Store` used directly
- `GiteaClient` — currently `gitea.Client` concrete type used directly
- `AgentSpawner` — exists in `relay/server.go:22`, move to port
- `Merger` — exists in `orchestration/cleanup.go:11`, move to port
- `PoolReturner` — exists in `orchestration/cleanup.go:18`, move to port
- `GitRunner` — exists in `pool/git.go:8`, move to port
- `Logger` — not defined, create per style guide

### Sentinel errors to define
- `ErrProjectNotFound`, `ErrProjectExists`
- `ErrAgentNotFound`
- `ErrPRTrackingNotFound`
- `ErrPoolExhausted`
- `ErrGiteaUnreachable`
- `ErrForbidden`

### Business logic to extract into services
- PR lifecycle: create tracking, handle review, escalate, merge — from `relay/server.go`
- Track discovery — from `orchestration/tracks.go`
- Merge and cleanup workflow — from `orchestration/cleanup.go`

## Acceptance Criteria

- [ ] `internal/core/domain/` contains all entity structs, value objects, and sentinel errors
- [ ] `internal/core/domain/` has zero imports from `internal/adapter/`
- [ ] `internal/core/port/` contains interfaces: `ProjectStore`, `AgentStore`, `GiteaClient`, `AgentSpawner`, `Merger`, `PoolReturner`, `Logger`
- [ ] `internal/core/port/doc.go` documents the not-found sentinel error convention
- [ ] `internal/core/service/` contains extracted business logic
- [ ] `internal/core/service/` imports only `domain/` and `port/`, never adapter packages
- [ ] Adapter packages implement port interfaces
- [ ] `internal/project/` and `internal/state/` merged into `internal/adapter/persistence/`
- [ ] `internal/orchestration/` split: business logic → service, persistence → adapter
- [ ] No dependency direction violations (verified by import inspection)
- [ ] All tests pass
- [ ] Build succeeds

## Dependencies

- refactor-clean-arch_20260307140000Z (package restructure must complete first)

## Blockers

- **test-coverage-alignment_20260307140002Z** — must wait for this track to complete (depends on domain/port/service layer existing)
- **Any new feature track** — should not start until this track is merged, since it restructures domain types, interfaces, and import paths across the entire codebase

## Conflict Risk

- **HIGH** — Moves types between packages, creates new interfaces, and updates imports throughout the codebase. No other track should be in-progress concurrently.

## Out of Scope

- Adding new tests (track 3)
- RBAC implementation (future — style guide documents the pattern for when it's needed)
- Structured logging migration (future — Logger interface enables it later)

## Technical Notes

### Extraction order
1. Define sentinel errors in `domain/errors.go`
2. Move entity structs to `domain/` (update imports everywhere)
3. Define port interfaces in `port/` (one file per interface)
4. Make existing adapter types implement the new interfaces
5. Update `relay/server.go` (now `adapter/rest/server.go`) to depend on port interfaces
6. Extract business logic from server.go into `service/` package
7. Remove old packages (`project/`, `state/`, `orchestration/`)

### Interface implementation verification
Add compile-time checks in each adapter:
```go
var _ port.ProjectStore = (*JSONProjectStore)(nil)
```

---

_Generated by conductor-track-generator_
