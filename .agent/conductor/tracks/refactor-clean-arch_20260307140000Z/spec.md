# Specification: Restructure Packages into Clean Architecture Layout

**Track ID:** refactor-clean-arch_20260307140000Z
**Type:** Refactor
**Created:** 2026-03-07T14:00:00Z
**Status:** Draft

## Summary

Reorganize the flat `internal/` package structure into the layered clean architecture defined in the Go style guide: `internal/core/{domain,port,service}` and `internal/adapter/{rest,gitea,compose,persistence,cli,agent,auth,pool}`.

## Context

The style guide mandates a strict layered architecture with dependency direction: domain → port → service → adapter. Currently all packages live flat under `internal/`, mixing domain types with I/O, scattering interfaces across adapters, and embedding business logic in HTTP handlers.

This track handles the **physical restructuring** — moving packages and updating imports. Domain/port/service content extraction is handled by a separate track.

## Codebase Analysis

Current flat structure:
```
internal/agent/      → adapter/agent/
internal/auth/       → adapter/auth/
internal/cli/        → adapter/cli/
internal/compose/    → adapter/compose/
internal/config/     → adapter/config/
internal/gitea/      → adapter/gitea/
internal/orchestration/ → Split later (track 2)
internal/pool/       → adapter/pool/
internal/project/    → Split later (track 2)
internal/relay/      → adapter/rest/
internal/state/      → Split later (track 2)
```

- Module path: `crelay` (go.mod)
- All imports use `crelay/internal/<pkg>` — must update to `crelay/internal/adapter/<pkg>` or `crelay/internal/core/<pkg>`

## Acceptance Criteria

- [ ] Directory structure matches: `internal/core/{domain,port,service}` and `internal/adapter/{rest,gitea,compose,persistence,cli,agent,auth,pool,config}`
- [ ] `internal/core/domain/` exists with placeholder `doc.go`
- [ ] `internal/core/port/` exists with placeholder `doc.go`
- [ ] `internal/core/service/` exists with placeholder `doc.go`
- [ ] Adapter packages moved: `cli/`, `compose/`, `config/`, `gitea/`, `agent/`, `auth/`, `pool/`
- [ ] `internal/relay/` renamed to `internal/adapter/rest/`
- [ ] `internal/project/` and `internal/state/` left in place (extracted in track 2)
- [ ] `internal/orchestration/` left in place (extracted in track 2)
- [ ] All imports updated across the entire codebase
- [ ] All tests pass
- [ ] Build succeeds

## Dependencies

None (all existing tracks are complete)

## Blockers

- **refactor-domain-ports_20260307140001Z** — must wait for this track to complete before starting (depends on new directory structure)
- **test-coverage-alignment_20260307140002Z** — must wait for this track (transitively blocked via track 2)
- **Any new feature track** — should not start until this track is merged, since every import path changes. Starting feature work concurrently will cause massive merge conflicts.

## Conflict Risk

- **HIGH** — This track modifies every `.go` file in the project (import path updates). No other track should be in-progress concurrently.

## Out of Scope

- Extracting domain types from adapter packages (track 2)
- Creating port interfaces (track 2)
- Extracting service layer from relay/orchestration (track 2)
- Adding new tests (track 3)

## Technical Notes

This is a pure move+rename refactor. No logic changes. The key risk is broken imports — every `crelay/internal/<old>` reference must be updated to `crelay/internal/adapter/<new>`.

Steps:
1. Create `internal/core/{domain,port,service}` directories with `doc.go`
2. `git mv` adapter packages one at a time, updating imports after each
3. Rename `relay` → `rest` during move
4. Leave `project/`, `state/`, `orchestration/` in place (track 2 handles splitting)
5. Run `go build ./...` and `go test ./...` after each move to catch issues early

---

_Generated by conductor-track-generator_
