id: command-deck-fe_20260311100002Z
title: "Rebrand dashboard to Command Deck — frontend titles, headers, and UI strings"
type: chore
status: pending
created: 2026-03-11
updated: 2026-03-11
spec:
  summary: |
    Rename all user-facing references to "Dashboard" in the frontend to
    "Command Deck". Update page titles, headers, sidebar branding, and any
    UI strings to use the new sci-fi-inspired name.
  context: |
    The dashboard UI is being rebranded to "Command Deck" — a sci-fi-inspired
    name for the real-time monitoring interface. This track handles the React
    frontend. The backend rename (Cortex) is handled separately.
  codebase_analysis: |
    User-facing "Dashboard" / "dashboard" references found in:
    - frontend/index.html — page title "Kiloforge Dashboard"
    - frontend/src/App.tsx — header/logo text
    - frontend/src/components/ — various component labels
    - frontend/src/pages/ — page titles and breadcrumbs
    The sidebar header shows "kiloforge" text. Page title in browser tab says
    "Kiloforge Dashboard". Various UI elements reference "dashboard".
  acceptance_criteria:
    - Browser tab title shows "Kiloforge Command Deck" (or similar)
    - App header/sidebar shows "Command Deck" branding
    - All user-facing strings reference "Command Deck" instead of "Dashboard"
    - No changes to internal component/file names (branding only)
    - Frontend builds without errors
    - All frontend tests pass
  out_of_scope: |
    - Backend changes (separate cortex-rebrand-be track)
    - Visual design overhaul (colors, layout, components)
    - Kiloforge_site changes (separate site-rebrand track)
    - Component/file renames (branding only)
  technical_notes: |
    This is a string replacement task in the React frontend. Only change
    user-facing text: page titles, headers, labels, tooltips, dialog copy.
    Do NOT rename React components, file paths, CSS classes, or route paths.
    Naming convention: "Command Deck" (two words, both capitalized).
    The header could show: "kiloforge" logo + "Command Deck" subtitle.
    Consider updating any empty state messages that reference "dashboard".
plan:
  - phase: "Core Branding"
    tasks:
      - text: "Update frontend/index.html page title to 'Kiloforge Command Deck'"
        done: false
      - text: "Update App.tsx header/sidebar branding text to reference Command Deck"
        done: false
      - text: "Update any navigation labels that say 'Dashboard' to 'Command Deck'"
        done: false
  - phase: "UI Strings"
    tasks:
      - text: "Search all frontend/src/ files for user-facing 'dashboard' or 'Dashboard' strings and update to 'Command Deck'"
        done: false
      - text: "Update dialog/modal titles that reference dashboard"
        done: false
      - text: "Update empty state messages and tooltips that reference dashboard"
        done: false
  - phase: "Verification"
    tasks:
      - text: "Grep frontend/src/ for remaining user-facing 'dashboard' references (exclude internal code, CSS, file paths)"
        done: false
      - text: "Run frontend build to verify no errors"
        done: false
      - text: "Run frontend tests to verify no breakage"
        done: false
extra: {}
