id: rename-relay-orchestrator_20260309075537Z
title: Rename Relay Server to Orchestrator
type: refactor
status: complete
created: 2026-03-09T07:55:37Z
updated: 2026-03-09T07:55:37Z
spec:
  summary: Rename the "relay server" to "orchestrator" throughout the codebase. The server's role has evolved from a simple webhook relay into a full development orchestration engine — managing agents, worktree pools, board state, tracing, locks, and the dashboard. The name should reflect what it actually does.
  context: |
    The relay server was originally conceived as a webhook relay between Gitea and Claude Code agents. It has since grown into:
    - Agent lifecycle management (spawn, monitor, halt, resume)
    - Worktree pool coordination
    - PR review cycle orchestration
    - Board state management (native kanban)
    - Scoped lock service
    - OpenTelemetry trace collection
    - Dashboard server with real-time SSE
    - Quota tracking and cost reporting
    
    "Relay" no longer describes this component. "Orchestrator" accurately reflects its role as the central coordinator of the kiloforge development workflow.
  codebase_analysis: |
    **Blast radius: ~30 files, 128 references**
    
    | Category | References | Files |
    |----------|-----------|-------|
    | Go config struct/fields (`RelayPort`) | 5 | 4 (config.go, defaults.go, flags_adapter.go, merger.go) |
    | CLI user-facing messages | 38 | 8 (serve.go, daemon.go, init.go, up.go, down.go, destroy.go, status.go, dashboard.go, add.go) |
    | REST server comments/logging | 5 | 1 (server.go) |
    | Gitea client (webhook relay port) | 2 | 1 (client.go) |
    | Compose template | 7 | 2 (template.go, template_test.go) |
    | AsyncAPI spec | 1 | 1 (asyncapi.yaml) |
    | README | 6 | 1 |
    | Log filename (`relay.log`) | 2 | 2 (serve.go, daemon.go) |
    | Env var (`KF_RELAY_URL`) | planned | skills + config |
    
    ### Key Rename Mappings
    
    | Before | After |
    |--------|-------|
    | `RelayPort` (struct field) | `OrchestratorPort` |
    | `relay_port` (JSON config) | `orchestrator_port` |
    | `WithRelayPort()` (flag option) | `WithOrchestratorPort()` |
    | `relay.log` (log file) | `orchestrator.log` |
    | `[relay]` (log prefix) | `[orchestrator]` |
    | "relay daemon" (CLI messages) | "orchestrator" |
    | "relay server" (docs) | "orchestrator" |
    | `KF_RELAY_URL` (env var) | `KF_ORCH_URL` |
    | `relayPort` (local vars) | `orchPort` |
    | `relayRunning` / `relayPID` | `orchRunning` / `orchPID` |
    | "Relay Server" (README) | "Orchestrator" |
    
    ### Naming Convention
    
    - **Full name in docs/UI:** "orchestrator"
    - **Short form in code vars:** `orch` (e.g., `orchPort`, `OrchestratorPort`)
    - **Env var:** `KF_ORCH_URL` (short, matches convention of `KF_*` being brief)
    - **Log file:** `orchestrator.log`
  acceptance_criteria:
    - Config struct field renamed from `RelayPort` to `OrchestratorPort`
    - JSON config key renamed from `relay_port` to `orchestrator_port`
    - Flag option renamed from `WithRelayPort` to `WithOrchestratorPort`
    - All CLI user-facing messages say "orchestrator" instead of "relay"
    - Log prefix changed from `[relay]` to `[orchestrator]`
    - Log filename changed from `relay.log` to `orchestrator.log`
    - REST server comments updated
    - Gitea client parameter renamed from `relayPort` to `orchPort`
    - Compose template field renamed
    - AsyncAPI spec description updated
    - README updated
    - Env var references updated to `KF_ORCH_URL`
    - All tests pass after rename
    - `make build` succeeds
  out_of_scope: |
    - The `rest/` package directory name stays — it describes the transport protocol, not the role
    - The Cobra subcommand `serve` stays — it's a verb, not a role name
    - Renaming the `daemon` package — it describes the process mode, not the role
    - Historical track IDs containing "relay" — track IDs are immutable timestamps
  technical_notes: |
    - **Config migration:** Users with existing `~/.kiloforge/config.json` containing `relay_port` will need the config reader to handle the old key gracefully. Add a simple fallback: if `orchestrator_port` is 0 and `relay_port` exists, use `relay_port`. Or just document the breaking change (this is pre-release software).
    - **Log file rename:** Existing `relay.log` files are not migrated — new logs go to `orchestrator.log`
    - **Compose template:** The `RelayPort` template field renames to `OrchestratorPort` — the compose YAML itself doesn't use the word "relay" (it's just a port number)
    
    ---
plan:
  - phase: Config Layer Rename
    tasks:
      - text: Rename `RelayPort` → `OrchestratorPort` in `config.go` struct + JSON tag (`orchestrator_port`)
        done: true
      - text: Rename `WithRelayPort()` → `WithOrchestratorPort()` in `flags_adapter.go`
        done: true
      - text: Update default value assignment in `defaults.go`
        done: true
      - text: Update merge logic in `merger.go`
        done: true
      - text: Update env adapter if `KF_RELAY_PORT` env var exists → `KF_ORCH_PORT`
        done: true
      - text: Update all config tests (`defaults_test.go`, `resolve_test.go`, `env_adapter_test.go`)
        done: true
      - text: Verify: `make test` passes for config package
        done: true
  - phase: CLI Messages and Logging
    tasks:
      - text: Update `serve.go` — log prefix `[relay]` → `[orchestrator]`, log file `relay.log` → `orchestrator.log`, startup message
        done: true
      - text: Update `daemon.go` — log file path, stop daemon comment
        done: true
      - text: Update `init.go` — all "relay" user-facing messages → "orchestrator"
        done: true
      - text: Update `up.go` — command description, all "relay" messages → "orchestrator"
        done: true
      - text: Update `down.go` — command description, all "relay" messages → "orchestrator"
        done: true
      - text: Update `destroy.go` — "relay daemon" messages → "orchestrator"
        done: true
      - text: Update `status.go` — command description, variable names (`relayRunning` → `orchRunning`)
        done: true
      - text: Update `dashboard.go` — command description referencing relay
        done: true
      - text: Update `add.go` — webhook relay message
        done: true
      - text: Verify: `make build` succeeds
        done: true
  - phase: REST Server and Gitea Client
    tasks:
      - text: Update `server.go` — comments, log prefix `[relay]` → `[orchestrator]`
        done: true
      - text: Update `client.go` — rename `relayPort` parameter to `orchPort` in `CreateWebhook`
        done: true
      - text: Update all callers of `CreateWebhook` that pass `RelayPort`
        done: true
      - text: Verify: `make test` passes for rest and gitea packages
        done: true
  - phase: Compose Template
    tasks:
      - text: Rename `RelayPort` → `OrchestratorPort` in compose template struct
        done: true
      - text: Update template string using `.RelayPort` → `.OrchestratorPort`
        done: true
      - text: Update `template_test.go` — struct field names and test descriptions
        done: true
      - text: Verify: `make test` passes for compose package
        done: true
  - phase: API Specs and Documentation
    tasks:
      - text: Update AsyncAPI spec description — "relay server" → "orchestrator"
        done: true
      - text: Update `README.md` — all "relay" references → "orchestrator"
        done: true
      - text: Update `backend/docs/architecture.md` if it references "relay"
        done: true
      - text: Update `backend/docs/commands.md` if it references "relay"
        done: true
      - text: Update `.agent/conductor/product.md` — product description
        done: true
  - phase: Environment Variable Rename
    tasks:
      - text: Rename `KF_RELAY_URL` → `KF_ORCH_URL` in env adapter
        done: true
      - text: Update env adapter tests
        done: true
      - text: Update skill files in `skills/` (if kf-skills-source track has completed) — `KF_RELAY_URL` → `KF_ORCH_URL`
        done: true
      - text: Search for any remaining `RELAY` references in env vars and update
        done: true
  - phase: Final Verification
    tasks:
      - text: Run `make build` — compiles cleanly
        done: true
      - text: Run `make test` — all tests pass
        done: true
      - text: Grep for remaining "relay" references — verify only historical research docs and track IDs remain
        done: true
      - text: Verify CLI messaging via `kf up --help`
        done: true
extra: "{}"