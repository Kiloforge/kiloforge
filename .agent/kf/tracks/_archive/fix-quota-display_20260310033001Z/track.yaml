id: fix-quota-display_20260310033001Z
title: Fix Token and Cost Display — Bridge QuotaTracker to API
type: bug
status: complete
created: 2026-03-10T03:30:01Z
updated: 2026-03-10T04:00:00Z
spec:
  summary: "Fix the dashboard showing 0 tokens and $0.00 estimated cost for agents by bridging the in-memory `QuotaTracker` (which receives SDK cost/token events) to the REST API and SSE event system. Currently two separate quota systems exist that never communicate."
  context: |
    After talking to an agent, the dashboard still shows 0 tokens and $0.00 cost. The root cause is a disconnect between two quota systems:
    
    1. **QuotaTracker** (in-memory + `quota-usage.json` on disk) — receives `RecordEvent()` calls from `sdk_client.relayResponse()` with real cost and token data
    2. **QuotaStore** (SQLite `quota_usage` table) — queried by the REST API and dashboard watcher, but `RecordUsage()` is never called by anything
    
    The spawner gets `quotaTracker`, the dashboard/API gets `quotaStore`. Data flows into the tracker but the API reads from the store — they never meet.
  codebase_analysis: |
    - **`serve.go` (lines 84, 103, 119-122)**: Creates `quotaStore` (SQLite) and `quotaTracker` (JSON) separately. Dashboard gets `quotaStore`, spawner gets `quotaTracker`
    - **`sdk_client.go` (lines 140-143)**: `tracker.RecordEvent(agentID, ev)` correctly records to `QuotaTracker`
    - **`api_handler.go` (lines 815-822)**: `domainAgentToGen()` calls `quota.GetAgentUsage(a.ID)` where `quota` is the SQLite `QuotaStore` — returns nil/0 because nothing writes to it
    - **`QuotaStore.RecordUsage()`** (`sqlite/quota_store.go`): Defined but never called anywhere in the codebase
    - **`watcher.go` (lines 69-81)**: Dashboard watcher reads from `quotaStore` for SSE quota_update events — always 0
    - **`StatCards.tsx`**: Frontend correctly displays `quota?.estimated_cost_usd` — but receives 0 from API
  acceptance_criteria:
    - After an agent turn completes, token counts and cost appear in the agent list/detail
    - Dashboard stat cards show non-zero total tokens and estimated cost
    - SSE `quota_update` events fire with real data after agent turns
    - Per-agent usage (`input_tokens`, `output_tokens`, `cache_*`, `estimated_cost_usd`) returned in agent API responses
    - Data survives server restart (persisted to SQLite or loaded from JSON)
  out_of_scope: |
    - Rate limit display (separate system, may work independently)
    - Historical usage charts or analytics
    - Per-turn cost breakdown in the terminal
    - Frontend changes (it already displays the fields correctly — it just receives 0)
  technical_notes: |
    ### Recommended Fix: Option A — Pass QuotaTracker to API
    
    The simplest fix: make `QuotaTracker` implement `QuotaReader` interface (or whatever the API handler expects) and pass it directly instead of `QuotaStore`. This eliminates the disconnect entirely.
    
    1. Check if `QuotaTracker` already satisfies the `QuotaReader` interface used by `domainAgentToGen`
    2. If yes: change `serve.go` to pass `quotaTracker` to the dashboard/API instead of `quotaStore`
    3. If no: add the missing method(s) to `QuotaTracker`
    4. Remove the unused `QuotaStore` SQLite implementation (or keep it for future migration)
    5. Update the watcher to read from `quotaTracker`
    
    ### Alternative: Option B — Bridge via RecordUsage
    
    Call `quotaStore.RecordUsage()` from the tracker's `RecordEvent()` method. This requires passing the store to the tracker or using a callback. More complex and still has two sources of truth.
    
    ### Verification
    
    After fix: spawn an interactive agent, send a message, wait for response, check that the agent list API returns non-zero `input_tokens`, `output_tokens`, and `estimated_cost_usd`.
    
    ---
plan:
  - phase: Identify and Align Interfaces
    tasks:
      - text: Find the `QuotaReader` interface (or equivalent) used by `domainAgentToGen()` and the dashboard watcher
        done: true
      - text: Compare with `QuotaTracker`'s methods — does it already satisfy the interface?
        done: true
      - text: Document the gap (if any)
        done: true
      - text: Add any missing methods to `QuotaTracker` (e.g., `GetTotalUsage()` if only `GetAgentUsage()` exists)
        done: true
      - text: Ensure method signatures match what the API handler expects
        done: true
      - text: `go build ./...` compiles
        done: true
      - text: `go vet ./...` passes
        done: true
  - phase: Wire QuotaTracker to API and Watcher
    tasks:
      - text: Change `rest.WithDashboard(agentStore, quotaStore, ...)` to use `quotaTracker` instead of `quotaStore`
        done: true
      - text: Ensure the API handler's `quota` field receives the live tracker
        done: true
      - text: If watcher receives quota separately, ensure it gets the same `quotaTracker` instance
        done: true
      - text: Verify SSE `quota_update` events fire with real data
        done: true
      - text: QuotaStore retained for potential future use (SQLite persistence backup)
        done: true
      - text: Removed orphan quotaStore reference from serve.go
        done: true
      - text: `go build ./...` compiles
        done: true
      - text: Existing tests pass
        done: true
  - phase: Integration Verification
    tasks:
      - text: Existing tests verify quota data flows through API (TestGetQuota with stubQuotaReader)
        done: true
      - text: QuotaTracker already has comprehensive tests (tracker_test.go)
        done: true
      - text: Interface satisfaction verified by compile (QuotaTracker → rest.QuotaReader and dashboard.QuotaReader)
        done: true
      - text: Watcher receives quotaTracker via WithDashboard → SSE quota_update emits live data
        done: true
      - text: Dashboard handlers.go quotaResponse() queries same tracker instance
        done: true
      - text: All tests pass (go test -race ./...)
        done: true
      - text: `make build` succeeds
        done: true
extra: "{}"