id: board-agent-lifecycle_20260308180000Z
title: Board-Driven Agent Lifecycle Control
type: feature
status: complete
created: 2026-03-08T18:00:00Z
updated: 2026-03-08T20:00:00Z
spec:
  summary: When a Gitea board card is moved backward (e.g., In Progress → Approved), halt the associated developer agent. When re-promoted forward, resume the agent. When an issue is closed without merge (rejected), terminate the agent and clean up the worktree.
  context: "The existing tracks (`impl-board-webhook-sync`) handle forward-flow board sync: tracks move Suggested → Approved → In Progress → In Review → Completed. However, there is no handling for backward movements or rejections. A user dragging a card from "In Progress" back to "Approved" should pause the developer agent, not leave it running on work that's been deprioritized."
  codebase_analysis: |
    - **Agent halt exists:** `AgentStore.HaltAgent(idPrefix)` sends SIGINT to the process PID (`internal/adapter/persistence/jsonfile/agent_store.go:118-133`)
    - **Agent resume exists:** `AgentSpawner.ResumeDeveloper(ctx, sessionID, workDir)` launches `claude --resume <sessionID>` (`internal/adapter/rest/server.go:129-133`)
    - **Agent lookup by track:** `AgentInfo.Ref` stores the track ID for developer agents. No `FindByRef()` method exists yet — only `FindAgent(idPrefix)` by agent ID prefix.
    - **Issue-to-track mapping:** The `impl-track-board-sync` track creates a `TrackIssue` mapping (track ID → Gitea issue number). This can be reversed to go from issue → track ID.
    - **Label-based detection:** `handleIssues()` already receives `label_updated` events (`internal/adapter/rest/server.go:272`). The `impl-board-webhook-sync` track will add status label parsing. This track extends that to detect *backward* label transitions.
    - **PR tracking links agents:** `PRTracking` (`internal/core/domain/pr_tracking.go`) links `DeveloperAgentID`, `ReviewerAgentID`, `DeveloperSession` to a track. Can be used to find and halt/resume agents in the review phase.
    - **Worktree cleanup exists:** `pool.ReturnByTrackID()` returns a worktree to the pool (`internal/adapter/pool/`).
    - **Agent states:** `halted` and `suspended` states exist in the domain model. The `halted` state is appropriate for user-initiated pauses via board.
  acceptance_criteria:
    - Moving a card from "In Progress" to "Approved" or "Suggested" halts the running developer agent
    - Moving a card from "In Review" backward halts both reviewer and developer agents
    - Moving a halted card back to "In Progress" resumes the developer agent
    - Closing an issue without a merged PR terminates the agent and returns the worktree to the pool
    - Adding a `rejected` label terminates the agent and posts a summary comment
    - All backward movements post an explanatory comment on the Gitea issue
    - Admin-user events are skipped (loop prevention, consistent with impl-board-webhook-sync)
    - Agent that cannot be resumed (expired session, missing worktree) is marked `resume-failed` with reason
  out_of_scope: |
    - Creating new agent states (use existing `halted`, `resume-failed`, `stopped`)
    - Modifying the board column structure (defined by impl-track-board-sync)
    - Automatic re-spawning of agents from scratch (only resume from existing session)
    - UI/dashboard changes for showing halted-by-board status
  technical_notes: |
    ### Column Transition Detection
    
    Status labels follow the pattern `status:<state>` (e.g., `status:in-progress`, `status:approved`). A backward movement is detected when:
    - `status:in-progress` is removed AND `status:approved` or `status:suggested` is added
    - `status:in-review` is removed AND any earlier-stage label is added
    
    The `label_updated` webhook payload includes the full current label set. Compare against the previous known state (from TrackIssue mapping or agent status) to determine direction.
    
    ### Agent Resolution Chain
    
    ```
    Issue webhook → extract issue number
      → TrackIssue mapping → track ID
      → AgentStore.FindByRef(trackID) → AgentInfo (developer)
      → PRTracking (if exists) → reviewer agent
    ```
    
    ### Halt vs Terminate
    
    - **Halt** (backward move): SIGINT → status `halted`, session preserved, can resume later
    - **Terminate** (rejection): SIGINT → status `stopped`, worktree returned to pool, no resume expected
    
    ---
plan:
  - phase: Agent Resolution by Track (3 tasks)
    tasks:
      - text: Add `FindByRef(ref string) *AgentInfo` method to `port.AgentStore` interface
        done: true
      - text: Implement in `jsonfile.AgentStore` — iterate agents, match on `Ref` field
        done: true
      - text: Return most recent matching agent (by `StartedAt`) if multiple exist
        done: true
      - text: Add unit test for FindByRef with single match, multiple matches, and no match
        done: true
      - text: Reverse-lookup: issue number → track ID via TrackIssue mapping
        done: true
      - text: Call `store.FindByRef(trackID)` to get the developer agent
        done: true
      - text: Return nil if no agent found (track not yet implemented)
        done: true
      - text: Load PRTracking for the project slug via prLoader function
        done: true
      - text: If PRTracking exists and `TrackID` matches, use for reviewer resolution
        done: true
      - text: Return nil if no PR exists yet for this track
        done: true
  - phase: Backward Movement Handlers (4 tasks)
    tasks:
      - text: Define column ordering: Suggested(0) < Approved(1) < InProgress(2) < InReview(3) < Completed(4)
        done: true
      - text: Compare previous column against new column to detect backward movement
        done: true
      - text: Skip if sender matches `config.GiteaAdminUser` (loop prevention via isSelfTriggered)
        done: true
      - text: Resolve agent via FindByRef(trackID)
        done: true
      - text: If agent status is `running` or `waiting`, call `store.HaltAgent(agent.ID)`
        done: true
      - text: Update agent status to `halted`, set ShutdownReason = "board-demotion"
        done: true
      - text: Post comment on issue, log the halt action
        done: true
      - text: Resolve both developer and reviewer agents
        done: true
      - text: Halt both agents if running
        done: true
      - text: Agent already completed/stopped → skip halt, log info
        done: true
      - text: Agent already halted → skip halt, idempotent
        done: true
      - text: Agent not found → skip, no-op
        done: true
      - text: Process already dead → mark halted anyway, log warning
        done: true
  - phase: Forward Re-promotion (3 tasks)
    tasks:
      - text: Detect forward move, check for halted developer agent
        done: true
      - text: Validate SessionID not empty, WorktreeDir exists
        done: true
      - text: Call spawner.ResumeDeveloper, update status to running
        done: true
      - text: Missing session → resume-failed
        done: true
      - text: Missing worktree → resume-failed
        done: true
      - text: Spawn error → resume-failed with ResumeError
        done: true
      - text: Log and skip (reviewer resume not directly supported)
        done: true
  - phase: Track Rejection and Cancellation (3 tasks)
    tasks:
      - text: Detect closed without merged PR
        done: true
      - text: Halt agent, set status to stopped, return worktree to pool
        done: true
      - text: Detect rejected label in handleLabelUpdated
        done: true
      - text: Terminate agent, close issue via Gitea API
        done: true
      - text: Already stopped/completed → skip termination
        done: true
      - text: No worktree assigned → skip pool return (nil pool check)
        done: true
  - phase: Tests (3 tasks)
    tasks:
      - text: All backward move scenarios tested
        done: true
      - text: All resume scenarios tested
        done: true
      - text: All rejection scenarios tested
        done: true
      - text: Full build: `go build -buildvcs=false ./...`
        done: true
      - text: Full test: `go test -buildvcs=false -race ./...`
        done: true
extra: "{}"