id: skill-ref-migration_20260309173002Z
title: "Migrate All Skill References from conductor-* to kf-*"
type: chore
status: complete
created: 2026-03-09T17:30:02Z
updated: 2026-03-09T17:24:21Z
spec:
  summary: "Update all backend code, documentation, and conductor artifacts to reference `kf-*` skill names instead of `conductor-*`. The `kf-skills-source` track already copied and rebranded skills into `skills/` as `kf-*`, but the Go source code (spawner, server, tests) and documentation still reference the old `conductor-*` names."
  context: "The `kf-skills-source` track rebranded 14 conductor skills to `kf-*` prefix in the repo's `skills/` directory. However, the actual code that spawns agents still uses the old names (`/conductor-developer`, `/conductor-reviewer`). This means agents will fail if only the kf-* skills are installed globally and the old conductor-* names are removed."
  codebase_analysis: |
    ### Critical Go source references (MUST fix)
    
    | File | Line | Current | Target |
    |------|------|---------|--------|
    | `backend/internal/adapter/agent/spawner.go` | 87 | `/conductor-reviewer` | `/kf-reviewer` |
    | `backend/internal/adapter/agent/spawner.go` | 154 | `conductor-developer flags` (comment) | `kf-developer flags` |
    | `backend/internal/adapter/agent/spawner.go` | 179 | `/conductor-developer` | `/kf-developer` |
    | `backend/internal/adapter/rest/server.go` | 154 | `/conductor-reviewer` | `/kf-reviewer` |
    | `backend/internal/adapter/skills/installer_test.go` | 44-45 | `conductor-developer/`, `conductor-reviewer/` | `kf-developer/`, `kf-reviewer/` |
    
    ### Documentation references
    
    | File | Description |
    |------|-------------|
    | `backend/docs/architecture.md` | Lines 59-60 — spawner command examples |
    | `backend/docs/design-agent-orchestration.md` | Lines 109, 169, 450 — agent prompts |
    | `backend/docs/getting-started.md` | Lines 130, 135 — CLI examples |
    | `README.md` | Line 179 — implements command description |
    
    ### Conductor artifacts
    
    | File | Description |
    |------|-------------|
    | `.agent/conductor/index.md` | Line 107 — "Getting Started" references `/conductor-track-generator` and `/conductor-new-track` |
    | `.agent/conductor/tracks.md` | Line 66 — comment `<!-- Tracks registered by /conductor-new-track -->` |
    
    ### Track specs (LOW priority — historical)
    
    ~90+ track spec files contain `_Generated by conductor-track-generator from prompt:` footer. These are historical records. Updating them is optional but covered by the existing `rebrand-historical-records` pending track.
  acceptance_criteria:
    - `spawner.go` uses `/kf-developer` and `/kf-reviewer` skill names
    - `server.go` uses `/kf-reviewer` skill name
    - `installer_test.go` uses `kf-developer/` and `kf-reviewer/` directory names
    - All backend docs updated to use kf-* names
    - README.md updated
    - `.agent/conductor/index.md` and `tracks.md` updated
    - `go test ./...` passes
    - `go build ./...` succeeds
  out_of_scope: |
    - Rebranding historical track spec footers (`_Generated by conductor-track-generator`) — covered by `rebrand-historical-records` track
    - Copying non-conductor utility skills (golang-pro, ui-design-standards, etc.) into the repo
    - Modifying the skills themselves (already rebranded)
  technical_notes: |
    This is a mechanical find-and-replace task. The spawner changes are the critical path — everything else is documentation.
    
    For the installer test, the tarball fixture creates `conductor-developer/SKILL.md` entries. Update to `kf-developer/SKILL.md` to match the actual skill directory names that would come from a kf-branded release tarball.
    
    ---
plan:
  - phase: Backend Source Code
    tasks:
      - text: Update `backend/internal/adapter/agent/spawner.go` — change `/conductor-developer` to `/kf-developer`, `/conductor-reviewer` to `/kf-reviewer`, update comment
        done: true
      - text: Update `backend/internal/adapter/rest/server.go` — change `/conductor-reviewer` to `/kf-reviewer`
        done: true
      - text: Update `backend/internal/adapter/skills/installer_test.go` — change `conductor-developer/` and `conductor-reviewer/` to `kf-developer/` and `kf-reviewer/`
        done: true
      - text: Search for any other conductor-* references in Go source — update if found
        done: true
  - phase: Documentation
    tasks:
      - text: Update `backend/docs/architecture.md` — fix spawner command examples
        done: true
      - text: Update `backend/docs/design-agent-orchestration.md` — fix agent prompt references
        done: true
      - text: Update `backend/docs/getting-started.md` — fix CLI examples
        done: true
      - text: Update `README.md` — fix implements command description
        done: true
  - phase: Conductor Artifacts
    tasks:
      - text: Update `.agent/conductor/index.md` — change Getting Started references to `/kf-track-generator` and `/kf-new-track`
        done: true
      - text: Update `.agent/conductor/tracks.md` — change comment to `<!-- Tracks registered by /kf-new-track -->`
        done: true
  - phase: Verification
    tasks:
      - text: Run `go test ./...` — all tests pass
        done: true
      - text: Run `go build ./...` — builds succeed
        done: true
      - text: Grep for remaining `conductor-` references in Go source (excluding _archive, track specs) — confirm none remain
        done: true
extra: "{}"