id: e2e-health-preflight-onboarding_20260309194831Z
title: E2E Tests: Health Check, Preflight Validation, and Onboarding Flow
type: chore
status: pending
created: 2026-03-09T19:48:31Z
updated: 2026-03-09
spec:
  summary: Comprehensive E2E tests for health check, preflight validation (Claude auth, skills, consent, setup), and the guided tour onboarding flow, covering happy path, edge cases, and expected failures.
  context: |
    The Kiloforge application has several startup and validation flows that must work correctly for users:
    
    1. **Health check** (`GET /api/health`) — basic server liveness
    2. **Status endpoint** — returns system info including version
    3. **Preflight validation** (`GET /api/preflight`) — checks Claude CLI auth, installed skills, consent state, and project setup
    4. **Consent dialog** — appears when agent permissions have not been granted
    5. **Skills install dialog** — appears when required skills are missing
    6. **Setup required dialog** — appears when no project is configured
    7. **Guided tour** — onboarding walkthrough for first-time users with step-by-step navigation and persistent state
    
    These flows are critical to the user experience and must be tested with both browser UI interactions (via Playwright) and direct API validation.
  codebase_analysis: |
    ### Existing patterns
    
    - **Health endpoint** — `GET /api/health` returns `{"status": "ok"}` in `backend/internal/adapter/rest/api_handler.go`
    - **Preflight endpoint** — `GET /api/preflight` checks auth, skills, consent, and setup status
    - **Consent API** — `GET /api/consent` and `PUT /api/consent` for agent permissions consent
    - **Skills API** — `GET /api/skills` and `POST /api/skills/install` for skill management
    - **Tour API** — `GET /api/tour`, `PUT /api/tour`, `GET /api/tour/demo-board` for guided tour state
    - **Frontend dialogs** — `ConsentDialog`, `SkillsInstallDialog`, `SetupRequiredDialog` components
    - **Tour component** — guided tour with step navigation, localStorage/API persistence
    
    ### Test infrastructure dependency
    
    All tests in this track rely on the E2E infrastructure from `e2e-infra-mock-agent_20260309194830Z`:
    - `startE2EServer()` for booting a test server with mock agent
    - `seedTestData()` for populating initial state
    - Playwright fixtures for browser-driven tests
  acceptance_criteria:
    - Health endpoint returns 200 with expected body
    - Status endpoint returns system info including version
    - Preflight endpoint validates all prerequisites (auth, skills, consent, setup)
    - ConsentDialog appears when permissions not granted, can be accepted
    - SkillsInstallDialog appears when skills missing
    - SetupRequiredDialog appears when project not setup
    - Guided tour starts on first visit, advances through steps, completes
    - Tour state persists across page reloads
    - Edge cases: concurrent preflight calls, rapid consent toggle
    - Failure cases: server down during preflight, partial skill installation failure
  out_of_scope: |
    - Project CRUD operations (covered by `e2e-project-management_20260309194832Z`)
    - Agent spawning and lifecycle (covered by `e2e-agent-lifecycle_20260309194834Z`)
    - Track management (covered by `e2e-track-management_20260309194833Z`)
  technical_notes: |
    ### Test file structure
    
    ```
    frontend/e2e/
      health-preflight.spec.ts    — health, status, preflight API tests
      consent-flow.spec.ts        — consent dialog UI tests
      onboarding-tour.spec.ts     — guided tour UI tests
    ```
    
    ### Preflight test scenarios
    
    | Scenario | Consent | Skills | Setup | Expected |
    |---|---|---|---|---|
    | All clear | granted | installed | complete | No dialogs, dashboard loads |
    | Missing consent | not granted | installed | complete | ConsentDialog shown |
    | Missing skills | granted | missing | complete | SkillsInstallDialog shown |
    | Missing setup | granted | installed | incomplete | SetupRequiredDialog shown |
    | Fresh install | not granted | missing | incomplete | ConsentDialog first, then skills, then setup |
    
    ### Tour test scenarios
    
    - Tour starts automatically on first visit (tour state = `pending`)
    - Tour step navigation: next, previous, skip to end
    - Tour completion sets state to `completed`
    - Tour dismissal sets state to `dismissed`
    - Reloading page resumes tour at current step (state = `active`)
    - Completed/dismissed tour does not restart on reload
    
    ### Edge case handling
    
    - Concurrent preflight: fire multiple `/api/preflight` requests simultaneously, verify no race conditions
    - Rapid consent toggle: grant and revoke consent rapidly, verify final state is consistent
    - Server error during preflight: mock server error response, verify UI shows appropriate error state
    
    ### Developer agent instructions
    
    Use the Playwright MCP skill to verify all UI interactions. Run tests in headed mode during development to visually confirm dialog appearances, tour step transitions, and state persistence across reloads.
    
    ---
plan:
  - phase: Health and Status Tests
    tasks:
      - text: Create `frontend/e2e/health-preflight.spec.ts` — test `GET /api/health` returns 200 with `{"status":"ok"}`, verify response headers and content type
        done: true
      - text: Test status endpoint — `GET /api/status` returns system info including version string, uptime, and configuration summary
        done: true
      - text: Test health endpoint failure — stop the backend, verify Playwright detects connection refused, verify UI shows offline/error state when navigating to dashboard
        done: true
  - phase: Preflight Tests
    tasks:
      - text: Test all-clear preflight — seed server with consent granted, skills installed, project setup complete; hit `GET /api/preflight`, verify all checks pass, verify dashboard loads without any blocking dialogs
        done: true
      - text: Test missing skills preflight — seed server with consent granted but skills not installed; verify preflight returns skills-missing status, verify SkillsInstallDialog appears in UI
        done: true
      - text: Test missing consent preflight — seed server with skills installed but consent not granted; verify preflight returns consent-missing status, verify ConsentDialog appears in UI
        done: true
      - text: Test missing setup preflight — seed server with consent and skills but no project configured; verify preflight returns setup-incomplete status, verify SetupRequiredDialog appears in UI
        done: true
  - phase: Consent Flow Tests
    tasks:
      - text: Test grant consent happy path — navigate to dashboard, verify ConsentDialog appears, click accept button, verify dialog dismisses, verify consent state persisted via `GET /api/consent`
        done: true
      - text: Test consent dialog UI interaction — verify dialog shows correct permission descriptions, verify cancel/dismiss behavior, verify keyboard accessibility (Escape key)
        done: true
      - text: Test consent state persistence — grant consent, reload page, verify ConsentDialog does not reappear, verify `GET /api/consent` returns granted state
        done: true
  - phase: Tour Tests
    tasks:
      - text: Create `frontend/e2e/onboarding-tour.spec.ts` — test tour auto-starts on first visit when tour state is `pending`, verify first step content is displayed, verify tour overlay/highlight is visible
        done: true
      - text: Test tour step navigation — advance through all tour steps using next button, verify each step shows correct content and highlights the correct UI element, test previous button navigates back
        done: true
      - text: Test tour completion — advance through all steps to final step, click complete/finish button, verify tour state changes to `completed` via `GET /api/tour`, verify tour UI dismissed
        done: true
      - text: Test tour state persistence across reload — start tour, advance to step 3, reload page, verify tour resumes at step 3 (not step 1), verify tour state shows `active` with `current_step: 3`
        done: true
  - phase: Edge and Failure Cases
    tasks:
      - text: Test concurrent preflight calls — fire 5 simultaneous `GET /api/preflight` requests using `Promise.all`, verify all return consistent results, verify no 500 errors or data corruption
        done: true
      - text: Test server errors during onboarding — mock API error on preflight endpoint, verify UI shows a user-friendly error message or retry option rather than a blank screen or unhandled exception
        done: true
      - text: Test partial failure scenarios — skills install endpoint returns partial success (some skills installed, some failed), verify UI shows which skills failed, verify retry is possible for failed skills only
        done: true
extra: {}