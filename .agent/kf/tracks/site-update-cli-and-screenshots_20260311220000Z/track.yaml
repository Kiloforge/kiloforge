id: site-update-cli-and-screenshots_20260311220000Z
title: Update site: remove kf init, replace outdated screenshots
type: chore
status: pending
created: 2026-03-11
updated: 2026-03-10
spec:
  summary: |
    Update the kiloforge_site "How it Works" section to show kf up / kf down
    workflow (kf init is removed), and replace all 3 feature screenshots that
    show the removed Gitea nav item with fresh captures from the current UI.
  context: |
    The CLI merged kf init into kf up — initialization now happens automatically
    on first run. The site at ../../kiloforge_site still references the old
    two-step "kf init then kf up" workflow. Additionally, all feature screenshots
    in public/features/ show "Gitea" in the nav bar, which was removed when
    Docker/Gitea support was dropped.
  codebase_analysis: |
    Site location: ../../kiloforge_site (Next.js 16, static export)
    
    Files to modify:
    - kiloforge_site/src/app/page.tsx (line ~367)
      - "How it Works" Step 1 says: "Start the Cortex control plane on your
        machine with `kf init`. Use `kf up` on subsequent runs."
      - Should become: "Start the Cortex control plane with `kf up`. Tear it
        down with `kf down`." (or similar)
    - kiloforge_site/public/features/overview.png — shows Gitea in nav
    - kiloforge_site/public/features/kanban-board.png — shows Gitea in nav
    - kiloforge_site/public/features/swarm-capacity.png — shows Gitea in nav
    
    The features page (src/app/features/page.tsx) references these 3 images.
    No code changes needed there — just replace the PNGs.
  acceptance_criteria:
    - No mention of "kf init" anywhere on the site
    - "How it Works" Step 1 describes kf up to start, kf down to stop
    - All 3 feature screenshots replaced with captures from current UI (no Gitea in nav)
    - Site builds successfully (npm run build)
  out_of_scope: |
    - Changing site layout, design, or adding new pages
    - Updating the features page descriptions (unless Gitea-specific)
    - Modifying the OG image
  technical_notes: |
    Approach:
    1. Update page.tsx line ~367 to replace kf init / kf up text with kf up / kf down
    2. Launch a local kiloforge instance (kf up) with a project that has agents/tracks
    3. Use Playwright or manual capture to screenshot:
       - Overview page (agents grid + gauge cards)
       - Board page (kanban with cards in various columns)
       - Swarm capacity page (project view with agent swarm section)
    4. Save screenshots to public/features/ replacing the 3 existing PNGs
    5. Run npm run build to verify static export succeeds
plan:
  - phase: Text Updates
    tasks:
      - text: Update page.tsx 'How it Works' Step 1 — replace 'kf init / kf up on subsequent runs' with 'kf up to start / kf down to stop'
        done: true
      - text: Grep entire site for any other references to 'kf init' or 'init' command and update
        done: true
  - phase: Screenshot Replacement
    tasks:
      - text: Capture fresh overview.png from running instance (agents grid + gauge cards, no Gitea in nav)
        done: true
      - text: Capture fresh kanban-board.png (board with cards in multiple columns, dependency lines visible)
        done: true
      - text: Capture fresh swarm-capacity.png (project page with agent swarm section)
        done: true
  - phase: Verification
    tasks:
      - text: Run npm run build in kiloforge_site — verify static export succeeds
        done: true
extra: {}