id: sdk-agent-migration-be_20260310014147Z
title: Migrate Agent Spawner to Claude Agent SDK (Backend)
type: refactor
status: complete
created: 2026-03-10T01:41:47Z
updated: 2026-03-10T02:18:00Z
spec:
  summary: "Replace the raw `exec.Command("claude")` + manual stream-json parsing in the agent spawner with the unofficial Go Claude Agent SDK (`github.com/schlunsen/claude-agent-sdk-go` v0.5.1), and enrich the WebSocket protocol with structured turn-based messages."
  context: |
    The current agent spawner manually constructs `exec.Command` invocations, pipes stdin/stdout, and scans stdout line-by-line to parse stream-json events. The parser (`parser.go`) only extracts raw text from `content_block_delta` and `assistant`/`message` events, ignoring tool use, thinking blocks, and system events. The WebSocket protocol is flat — only `output` (raw text), `status`, and `error` message types. This means the dashboard cannot display structured agent activity (tool calls, thinking, turn boundaries).
    
    The Claude Agent SDK provides:
    - `Client` with `Connect/Query/ReceiveResponse` lifecycle for interactive sessions
    - `Query()` function for one-shot (non-interactive) agents
    - Typed messages: `AssistantMessage` (with `TextBlock`, `ToolUseBlock`, `ThinkingBlock`), `SystemMessage`, `ResultMessage` (with cost/usage/session data), `StreamEvent`
    - Subprocess transport management, CLI discovery, environment cleaning
    - Session resumption via `WithResume(sessionID)`
  codebase_analysis: |
    - **`backend/internal/adapter/agent/spawner.go`** (~570 lines): Contains `SpawnReviewer`, `SpawnDeveloper`, `SpawnInteractive` — all build CLI args manually, create `exec.Command`, set up pipes, and monitor via goroutines. `CleanClaudeEnv()` duplicates SDK functionality. `monitorAgent` and `monitorInteractive` do line-by-line scanning.
    - **`backend/internal/adapter/agent/parser.go`** (~105 lines): `ExtractText()` only handles `content_block_delta` and `assistant`/`message` types. `ParseStreamLine()` extracts basic stream event metadata. These are replaced by SDK's `UnmarshalMessage` and typed content blocks.
    - **`backend/internal/adapter/ws/message.go`** (~38 lines): Flat protocol with 4 message types (`input`, `output`, `status`, `error`). Needs enrichment with structured message types.
    - **`backend/internal/adapter/ws/session.go`** (~143 lines): `StartOutputRelay` broadcasts raw `[]byte` text. Needs to broadcast structured JSON messages.
    - **`backend/internal/adapter/rest/api_handler.go`**: Calls `SpawnInteractive` — interface change needed if return type changes.
    - **`backend/internal/adapter/agent/tracker.go`**: Quota tracker uses `StreamEvent` from parser — needs to work with SDK `ResultMessage` for cost/usage.
  acceptance_criteria:
    - Interactive agents use SDK `Client` (Connect/Query/ReceiveResponse pattern)
    - Non-interactive agents (developer, reviewer) use SDK `Query()` function
    - `CleanClaudeEnv()` removed — SDK handles environment cleaning
    - SDK typed messages (AssistantMessage, SystemMessage, ResultMessage) parsed and forwarded
    - WS protocol extended with message types: `turn_start`, `text`, `tool_use`, `thinking`, `turn_end`, `system`
    - `turn_end` messages include cost/usage data from `ResultMessage`
    - Quota tracker updated to extract usage from SDK `ResultMessage`
    - Agent tracing spans updated with SDK message metadata
    - All existing tests pass or are updated for new interfaces
    - `parser.go` ExtractText/ParseStreamLine removed or deprecated (replaced by SDK)
    - Interactive agent stdin input triggers new SDK `Query()` call (turn-based)
  out_of_scope: |
    - Frontend changes (separate track: `sdk-agent-migration-fe_20260310014148Z`)
    - SDK library modifications or forking
    - Changes to non-agent REST endpoints
    - Agent persistence/recovery changes
  technical_notes: |
    ### Migration Strategy
    
    **Interactive agents (SpawnInteractive):**
    1. Create SDK `Client` with `types.NewClaudeAgentOptions()` configured from existing opts
    2. Call `client.Connect(ctx)` to start the subprocess
    3. If initial prompt provided, call `client.Query(ctx, prompt)`
    4. Range over `client.ReceiveResponse(ctx)` — type-switch on messages
    5. For each message, serialize to enriched WS protocol and broadcast
    6. When user sends input via WS, call `client.Query(ctx, userInput)` for next turn
    7. `ResultMessage` marks turn end — broadcast `turn_end` with cost data
    
    **Non-interactive agents (SpawnDeveloper, SpawnReviewer):**
    1. Use `claude.Query(ctx, prompt, opts)` one-shot function
    2. Range over returned channel for typed messages
    3. Log messages, track quota from `ResultMessage`
    4. No WS broadcast needed (these run headless)
    
    ### WS Protocol Extension
    
    New message types (all server → client):
    ```json
    {"type": "turn_start", "turn_id": "uuid"}
    {"type": "text", "text": "...", "turn_id": "uuid"}
    {"type": "tool_use", "tool_name": "Read", "tool_id": "toolu_xxx", "input": {...}, "turn_id": "uuid"}
    {"type": "thinking", "thinking": "...", "turn_id": "uuid"}
    {"type": "turn_end", "turn_id": "uuid", "cost_usd": 0.034, "usage": {...}}
    {"type": "system", "subtype": "init|warning|error", "data": {...}}
    ```
    
    Existing types preserved for backward compatibility:
    ```json
    {"type": "input", "text": "..."}     // client → server (unchanged)
    {"type": "status", "status": "..."}  // server → client (unchanged)
    {"type": "error", "message": "..."}  // server → client (unchanged)
    ```
    
    ### Quota Tracker Integration
    
    Replace `ParseStreamLine` → `StreamEvent` with SDK `ResultMessage`:
    - `ResultMessage.TotalCostUSD` → rate limit tracking
    - `ResultMessage.Usage` map → token counting
    - `ResultMessage.SessionID` → session correlation
    
    ---
plan:
  - phase: WS Protocol Extension
    tasks:
      - text: Add new message type constants to `ws/message.go`: `MsgTurnStart`, `MsgText`, `MsgToolUse`, `MsgThinking`, `MsgTurnEnd`, `MsgSystem`
        done: true
      - text: Add struct fields for turn-based data (turn_id, tool_name, tool_id, input, thinking, cost_usd, usage, subtype, data)
        done: true
      - text: Add constructor functions: `TurnStartMsg()`, `TextMsg()`, `ToolUseMsg()`, `ThinkingMsg()`, `TurnEndMsg()`, `SystemMsg()`
        done: true
      - text: Keep existing `OutputMsg`, `StatusMsg`, `ErrorMsg` for backward compat
        done: true
      - text: Write tests for each new message constructor
        done: true
      - text: Verify JSON roundtrip for all message types
        done: true
      - text: Verify backward compatibility: old `OutputMsg` still works
        done: true
  - phase: SDK Integration for Interactive Agents
    tasks:
      - text: Create `backend/internal/adapter/agent/sdk_client.go`
        done: true
      - text: Define `SDKSession` struct wrapping `claude.Client` with agent metadata
        done: true
      - text: Implement `NewSDKSession(ctx, opts)` that configures `types.ClaudeAgentOptions` from spawner config
        done: true
      - text: Handle: model, CWD, dangerously-skip-permissions, environment vars
        done: true
      - text: Map existing `SpawnInteractiveOpts` to SDK options
        done: true
      - text: Rewrite `SpawnInteractive` to create `SDKSession` instead of `exec.Command`
        done: true
      - text: Call `client.Connect(ctx)` to start the session
        done: true
      - text: If `opts.Prompt` set, call `client.Query(ctx, prompt)` for initial turn
        done: true
      - text: Return `InteractiveAgent` with adapted IO channels
        done: true
      - text: Update `InteractiveAgent` struct: replace `Stdin io.WriteCloser` with method-based input (`InputHandler`)
        done: true
      - text: Create `relayResponse(session, output chan)` goroutine
        done: true
      - text: Range over `client.ReceiveResponse(ctx)` channel
        done: true
      - text: Type-switch on SDK messages:
        done: true
      - text: Generate turn_id (UUID) per turn, emit `TurnStartMsg` at start
        done: true
      - text: When user sends WS `input` message, call `session.Query(ctx, text)` to start a new turn
        done: true
      - text: Start new `relayResponse` goroutine for the response
        done: true
      - text: Handle concurrent input rejection (one turn at a time)
        done: true
      - text: Add `NewSDKBridge` for SDK-based agents with `InputHandler` instead of stdin pipe
        done: true
      - text: Add `StartStructuredRelay` for pre-serialized JSON messages
        done: true
      - text: Ensure ring buffer stores structured messages for reconnection replay
        done: true
      - text: Test: SDK bridge WriteInput routes through InputHandler
        done: true
      - text: Test: structured relay buffers messages correctly
        done: true
      - text: Test: reconnection replays structured messages from ring buffer
        done: true
  - phase: SDK Integration for Non-Interactive Agents
    tasks:
      - text: Rewrite `SpawnDeveloper` to use `QueryOneShot()` which wraps `claude.Query(ctx, prompt, opts)`
        done: true
      - text: Configure SDK options: model, CWD, dangerously-skip-permissions
        done: true
      - text: Process messages in background goroutine via `runSDKAgent`
        done: true
      - text: Same pattern as SpawnDeveloper via `QueryOneShot()`
        done: true
      - text: Maintain existing prompt construction (`/kf-reviewer {prURL}`)
        done: true
      - text: Replace `monitorAgent` line-by-line scanning with SDK message processing in `QueryOneShot`
        done: true
      - text: Type-switch on messages for logging and quota tracking via `resultToStreamEvent`
        done: true
      - text: Extract cost/usage from `ResultMessage` for tracer spans
        done: true
      - text: Log structured messages to agent log files
        done: true
      - text: Test: `resultToStreamEvent` correctly converts SDK ResultMessage
        done: true
      - text: Test: `extractUsageInfo` maps SDK usage map to UsageInfo struct
        done: true
      - text: Test: `intFromMap` handles all numeric types
        done: true
  - phase: Cleanup and Integration
    tasks:
      - text: `resultToStreamEvent` bridges SDK `ResultMessage` → internal `StreamEvent` for `RecordEvent`
        done: true
      - text: Extracts `TotalCostUSD`, `Usage` map fields for rate limit tracking
        done: true
      - text: `ParseStreamLine`-based recording path preserved for backward compat
        done: true
      - text: `CleanClaudeEnv()` deprecated but retained (still used by server.go for non-SDK callers)
        done: true
      - text: `ExtractText()` and `ParseStreamLine()` preserved (still used by parser_test.go, quota tracker)
        done: true
      - text: Removed manual CLI arg construction from spawner
        done: true
      - text: Removed `monitorAgent` and `monitorInteractive` (replaced by SDK-based equivalents)
        done: true
      - text: Updated all `NewBridge(ia.Info.ID, ia.Stdin, ia.Done)` → `NewSDKBridge(ia.Info.ID, ia.Stdin, ia.Done)`
        done: true
      - text: Updated all `StartOutputRelay` → `StartStructuredRelay`
        done: true
      - text: WS handler `readLoop` calls `bridge.WriteInput()` which routes through `InputHandler`
        done: true
      - text: Span attributes set from SDK `ResultMessage` via `extractUsageInfo`
        done: true
      - text: Token counts from `ResultMessage.Usage` map
        done: true
      - text: Cost from `ResultMessage.TotalCostUSD`
        done: true
      - text: All tests pass (`make test`)
        done: true
      - text: Full build succeeds (`make build`)
        done: true
      - text: Quota tracking via `resultToStreamEvent` tested
        done: true
      - text: WS protocol structured messages tested
        done: true
  - phase: Documentation
    tasks:
      - text: WS protocol enriched with code comments documenting new message types
        done: true
      - text: `CleanClaudeEnv` marked deprecated with SDK migration note
        done: true
extra: "{}"