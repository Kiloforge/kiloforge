id: rebrand-kiloforge_20260309055250Z
title: Rebrand kiloforge to kiloforge (CLI: kf)
type: chore
status: complete
created: 2026-03-09T05:52:50Z
updated: 2026-03-09T05:52:50Z
spec:
  summary: Rename the product from "kiloforge" to "kiloforge" with the CLI binary becoming "kf". This is a full rebrand covering the Go module path, binary name, environment variables, data directory, frontend strings, API specs, and documentation.
  context: The product has outgrown the "kiloforge" name (which reflected its original webhook-relay focus). "kiloforge" better represents what the product has become — a full development orchestration forge. The CLI shorthand "kf" is simpler and faster to type than "kiloforge".
  codebase_analysis: |
    **Blast radius: ~150 files, 300+ references across these categories:**
    
    | Category | References | Files |
    |----------|-----------|-------|
    | Go module imports (`"kiloforge/..."`)) | 186 | 68 |
    | Environment variables (`KF_*`) | 24 | 3 |
    | Data directory paths (`~/.kiloforge`) | 78 | 15+ |
    | CLI command name (cobra root) | 2 | 1 |
    | Frontend strings (title, help) | 3 | 3 |
    | API spec titles | 2 | 2 |
    | Documentation | 40+ | 7+ |
    | Conductor metadata | 10+ | 10+ |
    
    **Key rename mappings:**
    
    | Before | After |
    |--------|-------|
    | `module kiloforge` | `module kiloforge` |
    | `cmd/kiloforge/` | `cmd/kf/` |
    | `kiloforge` (binary) | `kf` (binary) |
    | `KF_*` env vars | `KF_*` env vars |
    | `~/.kiloforge/` | `~/.kiloforge/` |
    | `kiloforge` (cobra Use) | `kf` |
    | `<title>kiloforge dashboard</title>` | `<title>kiloforge</title>` |
    | `Kiloforge API` (OpenAPI) | `Kiloforge API` |
    
    **Critical constraint:** The Go module rename must be atomic — go.mod + all import paths must change in one commit or the project won't compile.
  acceptance_criteria:
    - Go module is `kiloforge` and all imports updated
    - Binary builds as `kf` from `cmd/kf/main.go`
    - CLI root command shows `kf` in usage/help
    - Environment variables use `KF_` prefix (e.g., `KF_DATA_DIR`, `KF_API_TOKEN`)
    - Default data directory is `~/.kiloforge/`
    - Frontend displays "kiloforge" in title and UI
    - OpenAPI/AsyncAPI specs use "Kiloforge" title
    - README and all docs reference `kf` commands and `~/.kiloforge/` paths
    - Makefile builds `bin/kf` binary
    - All tests pass after rename
    - Conductor product.md and setup_state.json reflect new name
  out_of_scope: |
    - Migration tooling for existing `~/.kiloforge/` data directories (separate track if needed)
    - Git remote/repo URL renames (that's a hosting concern, not codebase)
    - Renaming archived conductor track titles (historical record — leave as-is)
    - Backward compatibility shims for old env var names
  technical_notes: |
    - **Go module rename:** Change `go.mod` module line, then bulk find-replace all `"kiloforge/` imports to `"kiloforge/`
    - **Directory rename:** `cmd/kiloforge/` → `cmd/kf/` (the binary name, not the module name)
    - **Env var prefix:** Use `KF_` (short, matches CLI name) rather than `KILOFORGE_` (too long)
    - **Data dir:** `~/.kiloforge/` — no migration from `~/.kiloforge/` in this track
    - **Makefile:** Update `BINARY` and build paths
    - **Case sensitivity:** Handle `kiloforge` (lowercase), `Kiloforge` (title case), and `KF` (upper case) separately
    - **Conductor skills:** The skills reference `KF_RELAY_URL` env var — this changes to `KF_RELAY_URL`. Update skill files accordingly.
    
    ---
plan:
  - phase: Go Module and Import Rename (Core — must be atomic)
    tasks:
      - text: Update `backend/go.mod` module line from `kiloforge` to `kiloforge`
        done: true
      - text: Bulk rename all Go import paths from `"kiloforge/` to `"kiloforge/` across all .go files
        done: true
      - text: Rename directory `backend/cmd/kiloforge/` to `backend/cmd/kf/`
        done: true
      - text: Update Makefile — binary name to `bin/kf`, build path to `./cmd/kf`
        done: true
      - text: Verify: `make build` succeeds
        done: true
  - phase: CLI Identity
    tasks:
      - text: Update cobra root command `Use` from `"kiloforge"` to `"kf"` in `root.go`
        done: true
      - text: Update cobra root command `Long` description to reference "kiloforge"
        done: true
      - text: Update any subcommand help text that references "kiloforge" (e.g., init.go, add.go help strings)
        done: true
      - text: Verify: `./bin/kf --help` shows correct branding
        done: true
  - phase: Environment Variables and Config Paths
    tasks:
      - text: Rename env var prefix from `KF_` to `KF_` in `env_adapter.go`
        done: true
      - text: Update default data directory from `~/.kiloforge` to `~/.kiloforge` in `defaults.go`
        done: true
      - text: Update env var references in `env_adapter_test.go`
        done: true
      - text: Update env var references in `resolve_test.go`
        done: true
      - text: Search for any other `KF_` references in Go code and update
        done: true
      - text: Verify: `make test` passes for config package
        done: true
  - phase: API Specifications
    tasks:
      - text: Update OpenAPI spec title from "Kiloforge API" to "Kiloforge API" in `backend/api/openapi.yaml`
        done: true
      - text: Update AsyncAPI spec title/description in `backend/api/asyncapi.yaml`
        done: true
      - text: Regenerate API code: `make gen-api`
        done: true
      - text: Verify: `make verify-codegen` passes
        done: true
  - phase: Frontend
    tasks:
      - text: Update page title in `frontend/index.html` to "kiloforge"
        done: true
      - text: Update heading in `frontend/src/App.tsx` from "kiloforge" to "kiloforge"
        done: true
      - text: Update help text in `frontend/src/pages/OverviewPage.tsx` — change `kf add` to `kf add`
        done: true
      - text: Search for any other "kiloforge" references in frontend/ and update
        done: true
      - text: Verify: frontend builds successfully
        done: true
  - phase: Documentation
    tasks:
      - text: Update `README.md` — all command examples, paths, and project description
        done: true
      - text: Update `backend/docs/architecture.md`
        done: true
      - text: Update `backend/docs/commands.md`
        done: true
      - text: Update `backend/docs/getting-started.md`
        done: true
      - text: Update `backend/docs/design-agent-orchestration.md` (if references exist)
        done: true
      - text: Update `backend/docs/research-docker-compose.md` (if references exist)
        done: true
      - text: Update `backend/docs/research-global-gitea-multiproject.md` (if references exist)
        done: true
  - phase: Conductor Metadata
    tasks:
      - text: Update `.agent/conductor/product.md` — project name and description
        done: true
      - text: Update `.agent/conductor/index.md` — navigation hub title
        done: true
      - text: Update `.agent/conductor/setup_state.json` — project_name field
        done: true
      - text: Update `.agent/conductor/product-guidelines.md` if it references "kiloforge"
        done: true
      - text: Update skill files that reference `KF_RELAY_URL` to `KF_RELAY_URL`
        done: true
  - phase: Final Verification
    tasks:
      - text: Run `make build` — binary compiles as `bin/kf`
        done: true
      - text: Run `make test` — all tests pass
        done: true
      - text: Run `make lint` — no lint errors
        done: true
      - text: Grep for any remaining "kiloforge" references (case-insensitive) — verify only historical track specs remain
        done: true
      - text: Run `./bin/kf --help` — verify CLI branding
        done: true
extra: "{}"