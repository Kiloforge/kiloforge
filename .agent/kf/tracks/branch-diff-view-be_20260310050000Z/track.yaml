id: branch-diff-view-be_20260310050000Z
title: Branch Diff View API (Backend)
type: feature
status: complete
created: 2026-03-10T05:00:00Z
updated: 2026-03-10T05:50:00Z
spec:
  summary: Add a REST API endpoint that returns structured git diffs between any branch and main, plus an endpoint to look up which agent is working on a given branch. This powers the frontend diff viewer that replaces Gitea's PR review UI.
  context: |
    The project is moving away from Gitea as the review/diff interface. Instead, the dashboard will provide a built-in diff viewer for any agent's working branch vs main. The backend needs to provide structured diff data (not raw patch text) so the frontend can render a rich file-by-file diff view.
    
    Agents work in worktrees with branches like `worker-1`, `worker-2`, etc., and create implementation branches named after their track ID. The pool already tracks which agent owns which worktree and branch.
  codebase_analysis: |
    - **Git operations** exist in `internal/adapter/git/sync.go` — currently only sync status (ahead/behind), no diff capability
    - **Pool** in `internal/adapter/pool/pool.go` — tracks worktree-to-agent-to-branch mappings via `Worktree.TrackID`, `Worktree.AgentID`, `Worktree.Branch`
    - **OpenAPI spec** at `backend/api/openapi.yaml` — all endpoints are schema-first with generated handler stubs
    - **REST handler** at `internal/adapter/rest/api_handler.go` — implements generated `ServerInterface`
    - **Agent domain** has `WorktreeDir` field linking agents to their worktree path
  acceptance_criteria:
    - `GET /api/projects/{slug}/diff?branch={branch}` returns structured diff JSON with file list, hunks, stats
    - Response includes per-file: path, status (added/modified/deleted/renamed), hunks with line numbers and content
    - Response includes summary stats: files changed, insertions, deletions
    - `GET /api/projects/{slug}/branches` returns list of active worktree branches with their agent IDs and track IDs
    - Diff endpoint handles edge cases: branch not found (404), no changes (empty diff), binary files (flagged but not diffed)
    - OpenAPI spec updated with new endpoints and response schemas
    - Unit tests for diff parsing logic
    - Integration tests for the endpoint
  out_of_scope: |
    - Frontend rendering (separate FE track)
    - Inline commenting or review annotations
    - Diff between arbitrary branches (only branch vs main)
    - Commit-by-commit diff (only cumulative diff)
  technical_notes: |
    ### Diff Service
    
    Create a new git diff adapter (`internal/adapter/git/diff.go`) that:
    1. Runs `git diff main...{branch} --stat` for summary stats
    2. Runs `git diff main...{branch} --no-color -U3` for unified diff output
    3. Parses the unified diff into structured `FileDiff` objects with hunks
    
    ### Structured Response
    
    ```json
    {
      "branch": "track-id-branch",
      "base": "main",
      "stats": {
        "files_changed": 5,
        "insertions": 120,
        "deletions": 30
      },
      "files": [
        {
          "path": "internal/core/service/foo.go",
          "old_path": "",
          "status": "modified",
          "insertions": 20,
          "deletions": 5,
          "is_binary": false,
          "hunks": [
            {
              "old_start": 10,
              "old_lines": 7,
              "new_start": 10,
              "new_lines": 12,
              "header": "@@ -10,7 +10,12 @@ func Foo() {",
              "lines": [
                {"type": "context", "content": " existing line", "old_no": 10, "new_no": 10},
                {"type": "delete", "content": "-removed line", "old_no": 11, "new_no": null},
                {"type": "add", "content": "+added line", "old_no": null, "new_no": 11}
              ]
            }
          ]
        }
      ]
    }
    ```
    
    ### Branch Listing
    
    Leverage `pool.Pool` to enumerate active worktrees with their branch, agent ID, and track ID. Expose via a simple endpoint so the frontend can show a branch picker.
    
    ### Diff Parsing
    
    Write a standalone diff parser (`internal/adapter/git/diffparse.go`) that converts unified diff text into the structured format. This is pure string parsing with no external dependencies — test it thoroughly with table-driven tests covering: new files, deleted files, renamed files, binary files, empty diffs, files with no newline at EOF.
    
    ---
plan:
  - phase: Diff Parser and Git Adapter (Foundation)
    tasks:
      - text: Create `internal/core/domain/diff.go` with types: `DiffResult`, `FileDiff`, `Hunk`, `DiffLine`, `DiffStats`, `LineSide` (add/delete/context)
        done: true
      - text: Include `BranchInfo` type for branch listing response
        done: true
      - text: Create `internal/adapter/git/diffparse.go` — pure function `ParseUnifiedDiff(raw string) ([]FileDiff, error)`
        done: true
      - text: Create `internal/adapter/git/diffparse_test.go` with table-driven tests:
        done: true
      - text: Parse `@@ -old,count +new,count @@` headers for line numbers
        done: true
      - text: Classify lines: `+` → add, `-` → delete, ` ` → context
        done: true
      - text: Track per-file insertion/deletion counts
        done: true
      - text: Create `internal/adapter/git/diff.go` with:
        done: true
      - text: Use `exec.CommandContext` with timeout (30s) to prevent hanging on large diffs
        done: true
      - text: Handle branch-not-found error (git exit code)
        done: true
      - text: Run diff parser tests — all passing
        done: true
      - text: Verify diff adapter compiles and builds
        done: true
  - phase: API Endpoints (Core)
    tasks:
      - text: Add `GET /api/projects/{slug}/diff` with query params `branch` (required) and `max_files` (optional)
        done: true
      - text: Add `GET /api/projects/{slug}/branches`
        done: true
      - text: Add all response schemas: DiffResponse, FileDiff, DiffHunk, DiffLine, DiffStats, BranchInfo
        done: true
      - text: Run oapi-codegen to regenerate handler interface and types
        done: true
      - text: Verify new methods appear in `StrictServerInterface`
        done: true
      - text: In `api_handler.go`, implement `GetProjectDiff`:
        done: true
      - text: In `api_handler.go`, implement `GetProjectBranches`:
        done: true
      - text: Test diff endpoint error cases (project not found, provider not configured)
        done: true
      - text: Test branches endpoint (project not found, agents with/without worktrees, empty)
        done: true
      - text: All tests passing
        done: true
      - text: `make test` clean
        done: true
      - text: `make build` clean
        done: true
  - phase: Integration and Polish
    tasks:
      - text: Create `DiffProvider` interface in `internal/core/port/diff_provider.go`
        done: true
      - text: Wire into REST handler via dependency injection (consistent with existing patterns)
        done: true
      - text: `max_files` query param (default 100) limits response size
        done: true
      - text: Truncated response includes `truncated: true` flag
        done: true
      - text: Binary files: included in file list with `is_binary: true` but empty hunks
        done: true
      - text: All endpoint tests pass
        done: true
      - text: Diff parser thoroughly tested with 8 table-driven test cases
        done: true
      - text: All tests passing
        done: true
      - text: `make test` clean
        done: true
      - text: `make build` clean
        done: true
extra: "{}"