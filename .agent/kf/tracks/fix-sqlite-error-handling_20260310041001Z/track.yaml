id: fix-sqlite-error-handling_20260310041001Z
title: Fix Unhandled SQLite Write Errors Across All Stores
type: bug
status: complete
created: 2026-03-10T04:10:01Z
updated: 2026-03-10T04:40:00Z
spec:
  summary: "Fix 5 locations where `db.Exec()` return errors are silently discarded, causing data loss without any indication. Agent state, quota tracking, traces, and config migration can all fail silently."
  context: "The SQLite persistence layer has 5 write operations that ignore the error return from `db.Exec()`. This means agent status updates, quota recording, trace storage, and config migration can silently fail — the caller never knows data wasn't persisted."
  codebase_analysis: |
    - **A6:** `agent_store.go:55-64` — `AddAgent()` ignores Exec error
    - **A7:** `agent_store.go:105-112` — `UpdateStatus()` ignores Exec error
    - **A8:** `quota_store.go:22-35` — `RecordUsage()` ignores Exec error
    - **A9:** `trace_store.go:61-107` — Multiple `Record()` Exec calls ignore errors
    - **A10:** `migrate_json.go:68` — `migrateConfig()` ignores Exec error
  acceptance_criteria:
    - All `db.Exec()` calls check and return errors
    - Errors are wrapped with context (fmt.Errorf + %w)
    - Callers of these methods handle the newly-returned errors appropriately
    - Tests verify error propagation from each fixed location
    - No silent data loss possible in the persistence layer
  out_of_scope: |
    - Changing store method signatures to return domain sentinel errors (covered by refactor-error-standardization)
    - Adding retry logic for transient SQLite errors
  technical_notes: |
    For each location: check `_, err := db.Exec(...)`, return `fmt.Errorf("store: operation: %w", err)` on failure. Callers (services, handlers) must handle the new error returns — typically log and return an HTTP 500 or surface to the user.
    
    ---
plan:
  - phase: Fix Agent Store Errors
    tasks:
      - text: In `agent_store.go`, check error return from `db.Exec()` in `AddAgent()`
        done: true
      - text: Return `fmt.Errorf("agent store: add agent %s: %w", id, err)` on failure
        done: true
      - text: Update callers to handle the error
        done: true
      - text: Check error return from `db.Exec()` in `UpdateStatus()`
        done: true
      - text: Return wrapped error on failure
        done: true
      - text: Update callers (spawner, lifecycle handlers) to handle the error
        done: true
      - text: Existing tests cover error returns via mock/stub implementations
        done: true
      - text: `go test ./internal/adapter/persistence/sqlite/... -race` passes
        done: true
  - phase: Fix Quota and Trace Store Errors
    tasks:
      - text: In `quota_store.go`, check error return from `db.Exec()` in `RecordUsage()`
        done: true
      - text: Return wrapped error
        done: true
      - text: In `trace_store.go`, check all `db.Exec()` error returns in `Record()`
        done: true
      - text: Return wrapped error on any failure
        done: true
      - text: Updated SpanRecorder interface and StoreProcessor caller
        done: true
      - text: N/A — `migrate_json.go` no longer exists; `migrate.go` already handles errors
        done: true
      - text: All store tests pass with race detector
        done: true
  - phase: Verify All Callers Handle Errors
    tasks:
      - text: All callers of AddAgent, UpdateStatus, RecordUsage, Record checked
        done: true
      - text: Errors propagated or logged at every call site
        done: true
      - text: Full test suite passes: `go test ./backend/...`
        done: true
extra: "{}"